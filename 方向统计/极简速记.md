# ***信息安全保护等级五级（极简速记）

# 一、五级（从低到高）

```text
1. 用户自主保护级
2. 系统审计保护级
3. 安全标记保护级
4. 结构化保护级
5. 访问验证保护级
```

---

# 二、最高级（超高频）

```text
访问验证保护级
```

即：

```text
第5级
```

---

# 三、软考口诀

```text
自主
审计
标记
结构
访问验证
```

---

# 四、真题

最高等级是：

```text
D 访问验证保护级
```

# ***净室软件工程（极简速记｜软考标准版）

# 一、核心思想

```text
用数学方法开发高质量软件
```

目标：

```text
以合理成本实现接近零缺陷
```

------

# 二、理论基础（超高频）

```text
函数理论
+
抽样理论
```

------

# 三、四大技术（高频）

```text
1. 增量开发
2. 函数规范与设计
3. 正确性验证
4. 统计测试与认证
```

------

# 四、核心技术

```text
正确性验证
```

这是：

```text
净室核心
```

------

# 五、真题（超高频）

错误说法：

```text
无需传统模块测试
```

答案：

```text
B
```

因为：

```text
完全不做传统模块测试
不现实
```

------

# 六、口诀

```text
函数 + 抽样
正确性验证最重要
```

# ***UML 用例图关系（极简速记）

# 一、真题

在 UML 用例图中，不属于用例与用例之间关系的是（ ）。

A 扩展关系
B 聚合关系
C 包含关系
D 继承关系

------

# 二、答案

```text
B 聚合关系
```

------

# 三、用例之间的三种关系（超高频）

```text
1. 包含（include）
2. 扩展（extend）
3. 泛化/继承（generalization）
```

------

# 四、聚合关系属于什么

```text
类图关系
```

不是：

```text
用例图关系
```

------

# 五、口诀

```text
用例：包含、扩展、继承
聚合属于类图
```

# ***数字孪生生态系统（极简速记）

# 一、数字孪生四层

```text
1. 数据互动层
2. 模型构建层
3. 仿真分析层
4. 共性应用层
```

------

# 二、核心功能

| 层         | 核心                   |
| ---------- | ---------------------- |
| 数据互动层 | 数据采集               |
| 模型构建层 | 建模仿真               |
| 仿真分析层 | 分析推演               |
| 共性应用层 | 描述、诊断、预测、决策 |

------

# 三、真题

```text
描述、诊断、预测、决策
=
共性应用层
```

------

# 四、口诀

```text
数据
模型
仿真
应用
```

# ***EAI 四层服务（极简速记）

# 一、EAI 四层（从下到上）

```text
1. 通信服务
2. 信息传递与转换服务
3. 应用连接服务
4. 流程控制服务
```

------

# 二、核心理解

```text
先通信
再转数据
再连应用
最后管流程
```

------

# 三、口诀

```text
通信
转换
连接
流程
```

# 嵌入式系统两种结构

```
嵌入式两大架构：
层次化
递归化
```

# 构件是独立的软件单元

```text
构件是独立的软件单元，
通常以二进制形式存在，
无需在部署前编译，
构件元数据是构件本身相关的数据。
构件是可以配置的，以适应不同的应用系统需求
```

# REST 风格（极简速记）

# 一、REST 核心

```text
REST 是一种设计风格
```

核心：

```text
以资源为中心
```

------

# 二、状态转移

通过：

```text
HTTP 方法实现
```

例如：

```text
GET
POST
PUT
DELETE
```

------

# 三、URI 与资源（超高频）

```text
多对一关系
```

即：

```text
一个资源
可以有多个 URI
```

------

# 四、真题

错误选项：

```text
URI 和资源是多对多关系
```

答案：

```text
B
```

------

# 五、口诀

```text
REST 是风格
资源是核心
HTTP 做操作
URI 多对一
```

# 黑板体系结构风格（极简速记）

# 一、黑板风格属于（超高频）

```text
数据中心风格
```

------

# 二、核心思想

```text
共享黑板
+
多个知识源协同求解
```

------

# 三、经典风格分类

| 大类          | 子风格        |
| ------------- | ------------- |
| 数据流风格    | 管道-过滤器   |
| 调用/返回风格 | 主程序-子程序 |
| 独立构件风格  | 进程通信      |
| 虚拟机风格    | 解释器        |
| 数据中心风格  | 仓库、黑板    |

------

# 四、口诀

```text
黑板和仓库
都属于数据中心
```

# DSSA 领域分析（领域特定软件体系结构）

# 一、真题

在 DSSA 中，（ ）阶段的主要目标是获得领域模型。

答案：

```text
D 领域分析
```

------

# 二、DSSA 三阶段（超高频）

| 阶段     | 核心目标       |
| -------- | -------------- |
| 领域分析 | 获得领域模型   |
| 领域设计 | 获得 DSSA      |
| 领域实现 | 开发可复用构件 |

------

# 三、口诀

```text
分析得模型
设计得架构
实现得构件
```

------

# 四、一句话总结

> DSSA 中，领域分析阶段的主要目标是获得领域模型。



# 敏捷开发核心思想（极简速记）

# 一、真题

以下关于敏捷开发核心思想说法错误的是（ ）。

答案：

```text
C：敏捷方法是适应型、可预测型
```

------

# 二、为什么错

敏捷开发：

```text
是适应型
不是可预测型
```

核心：

```text
快速响应变化
```

------

# 三、敏捷核心思想（超高频）

```text
1. 迭代增量开发
2. 快速响应变化
3. 以人为本
4. 持续交付
```

------

# 四、关键词

```text
适应变化
小步快跑
快速反馈
```

------

# 五、口诀

```text
敏捷重变化
不是重预测
```

# 系统测试依据（极简速记）

# 一、真题

系统测试的依据是（ ）。

答案：

```text
B 软件需求规格说明书
```

------

# 二、测试阶段对应依据（超高频）

| 测试阶段 | 依据           |
| -------- | -------------- |
| 单元测试 | 详细设计说明书 |
| 集成测试 | 概要设计说明书 |
| 系统测试 | 需求规格说明书 |
| 验收测试 | 用户需求       |

------

# 三、口诀

```text
单元看详细
集成看概要
系统看需求
```

# 采样连续时间信号

根据奈奎斯特采样定理，要不失真的恢复一个连续信号，采样频率必须大于信号最高频率的两倍。

```text
因此，如果采样频率为3KHz，则该连续信号的最高频率可能为1.5KHz。
```

------

# 数据库范式（极简速记）

# 一、真题

只消除非主属性对码的部分依赖的范式是（ ）。

答案：

```text
C 2NF
```

------

# 二、各范式核心（超高频）

| 范式 | 核心               |
| ---- | ------------------ |
| 1NF  | 属性不可再分       |
| 2NF  | 消除部分依赖       |
| 3NF  | 消除传递依赖       |
| BCNF | 更严格消除依赖异常 |

------

# 三、部分依赖

例如：

```text
(A,B) 是主码
A → C
```

说明：

```text
C 只依赖部分码 A
```

即：

```text
部分依赖
```

------

# 四、口诀

```text
1NF 不可分
2NF 消部分
3NF 消传递
BCNF 更严格
```

# UML中（ ）不属于需求分析常用的图

```text
在UML中，需求分析常用的图包括用例图、活动图和类图。用例图用于描述系统的功能需求及其外部因素；活动图用于表示系统行为或业务流程；类图用于展示系统的静态结构。而构件图（组件图）主要用于系统设计和实现阶段，表示系统中各个物理模块之间的关系，因此不属于需求分析阶段常用的图。
```

------

# 数据库设计阶段（极简速记）

# 一、真题

关系反规范化在哪个阶段进行？

答案：

```text
C 逻辑设计
```

------

# 二、数据库设计四阶段（超高频）

| 阶段     | 核心                       |
| -------- | -------------------------- |
| 需求分析 | 收集需求                   |
| 概念设计 | ER 图                      |
| 逻辑设计 | 关系模式、规范化、反规范化 |
| 物理设计 | 存储结构与索引             |

------

# 三、逻辑设计阶段（高频）

包括：

```text
1. ER图转关系模式
2. 关系模式规范化
3. 完整性约束
4. 用户视图
5. 反规范化设计
```

------

# 四、口诀

```text
逻辑阶段最重要
规范反规范都在这
```

# 静态测试与动态测试（极简速记）

# 一、静态测试（超高频）

核心：

```text
不运行程序
```

通过：

```text
分析文档、代码、流程图找错误
```

典型：

```text
代码审查
文档审查
流程分析
```

------

# 二、动态测试（超高频）

核心：

```text
运行程序进行测试
```

通过：

```text
执行软件发现错误
```

典型：

```text
单元测试
集成测试
系统测试
验收测试
```

------

# 三、黑盒 / 白盒 / 灰盒

| 测试 | 核心         |
| ---- | ------------ |
| 黑盒 | 看输入输出   |
| 白盒 | 看代码逻辑   |
| 灰盒 | 两边都看一点 |

------

# 四、真题（高频）

```text
静态测试：分析文档和代码
灰盒测试：既看输入输出，也看部分内部逻辑
```

------

# 五、口诀

```text
静态不运行
动态要执行
黑盒看功能
白盒看代码
灰盒两边都看一点
```

# TBAC 访问控制模型（极简速记）

# 一、TBAC 是什么

TBAC：

```text
Task-Based Access Control
```

中文：

```text
基于任务的访问控制
```

核心：

```text
权限随任务动态变化
```

------

# 二、TBAC 四组成（超高频）

| 组成       | 作用         |
| ---------- | ------------ |
| 工作流     | 任务流程     |
| 授权结构体 | 授权关系     |
| 受托人集   | 执行任务的人 |
| 许可集     | 权限集合     |

------

# 四、核心思想

```text
执行任务时获得权限
任务结束回收权限
```

------

# 五、口诀

```text
工作流
授权体
受托人
许可集
```



# SSL是一种安全套接层协议（极简速记）

```text
SSL是一种安全套接层协议，是 Web 浏览器与 Web 服务器之间安全交换信息的协议
```

核心：

```text
提供两个基本的安全服务:鉴别与保密。
SSL协议的三个特性。
①保密性:在握手协议中定义了会话密钥后，所有的消息都被加密:
②可靠性:可选的客户端认证，和强制的服务器端认证:
③完整性:传送的消息包括消息完整性检查(使用MAC)。
```

注意：

```text
不包括不可抵赖性。不可抵赖性由数字签名保证。SSL本身不提供此项功能。 
```

------

# 星型拓扑结构（极简速记）

# 一、核心概念

```text
所有节点连接到中心节点
```

结构像星星，因此叫星型拓扑。中心设备通常是交换机或集线器。

------

# 二、核心特点

| 特点     | 说明                 |
| -------- | -------------------- |
| 中心化   | 所有通信经过中心节点 |
| 易扩展   | 增加节点方便         |
| 易维护   | 故障容易定位         |
| 单点故障 | 中心坏则全网瘫痪     |

------

# 三、工作方式

```text
节点间不能直接通信，必须通过中心设备转发
```

------

# 四、优缺点

## 优点

```text
结构简单
易管理
易扩展
```

## 缺点

```text
依赖中心节点，中心故障全网停
```

------

# 五、与总线型对比

| 对比     | 星型         | 总线型       |
| -------- | ------------ | ------------ |
| 中心节点 | 有           | 无           |
| 故障影响 | 中心影响全网 | 主干影响全网 |
| 扩展性   | 强           | 一般         |
| 管理性   | 好           | 较差         |

------

# 六、典型应用

```text
局域网（LAN）
现代以太网大多采用星型拓扑
```

------

# 七、软考口诀

```text
所有节点连中心
中心故障全网停
```

------

# 八、一句话总结

> 星型拓扑中所有节点连接到中心节点，易管理易扩展，但中心故障会导致全网失效。

# 鸿蒙操作系统（极简速记）

# 一、真题

以下关于鸿蒙操作系统的叙述中，不正确的是（ ）。

答案：

```text
B
```

------

# 二、错误点

B 说：

```text
鸿蒙内核层采用宏内核设计
```

错误。

软考记忆：

```text
鸿蒙采用微内核设计思想
```

特点：

```text
安全性高
低时延
可扩展
```

------

# 三、鸿蒙四层架构

从下到上：

```text
1. 内核层
2. 系统服务层
3. 框架层
4. 应用层
```

------

# 四、分布式四能力

```text
1. 分布式软总线
2. 分布式设备虚拟化
3. 分布式数据管理
4. 分布式任务调度
```

------

# 五、安全三正确

```text
正确的人
通过正确的设备
正确地使用数据
```

------

# 六、口诀

```text
鸿蒙分四层
内核微内核
分布式四能力
安全三正确
```

# 系统可靠性（极简速记）

```text
可靠性：指在规定的时间内和规定条件下能有效地实现规定功能的能力。
      它不仅取决于规定的使用条件等因素，还与设计技术有关。
常用的度量指标主要有故障率（或失效率）、平均失效等待时间、平均失效间隔时间和可靠度等。
其中，可靠度是系统在规定工作时间内无故障的概率。 
可用性是系统能够正常运行的时间比例。经常用两次故障之间的时间长度或出现故障时系统能够恢复正常的速度来表示。 
可测试性：是指验证软件程序正确的难易程度。可测试性好的软件，通常意味着软件设计简单，复杂性低。因为软件的复杂性越大，测试的难度也就越大。 
可理解性：通过阅读源代码和相关文档，了解程序功能及其如何运行的容易程度
```

# 云计算服务模式：SaaS / PaaS / IaaS（极简速记）

# 一、三种服务模式

```text
IaaS：基础设施即服务
PaaS：平台即服务
SaaS：软件即服务
```

从底到上：

```text
IaaS → PaaS → SaaS
基础设施 → 平台 → 软件
```

------

# 二、IaaS

英文：

```text
Infrastructure as a Service
```

中文：

```text
基础设施即服务
```

提供：

```text
服务器、存储、网络、虚拟机
```

用户负责：

```text
操作系统、数据库、应用部署
```

口诀：

```text
IaaS = 租机器
```

------

# 三、PaaS

英文：

```text
Platform as a Service
```

中文：

```text
平台即服务
```

提供：

```text
运行环境、数据库、中间件、开发平台
```

用户负责：

```text
开发应用、上传代码
```

口诀：

```text
PaaS = 租平台
```

------

# 四、SaaS

英文：

```text
Software as a Service
```

中文：

```text
软件即服务
```

提供：

```text
可直接使用的软件
```

用户负责：

```text
直接使用
```

口诀：

```text
SaaS = 用软件
```

------

# 五、对比表

| 模式 | 中文           | 提供什么           | 用户做什么     |
| ---- | -------------- | ------------------ | -------------- |
| IaaS | 基础设施即服务 | 服务器、存储、网络 | 部署系统和应用 |
| PaaS | 平台即服务     | 开发运行平台       | 开发应用       |
| SaaS | 软件即服务     | 现成软件           | 直接使用       |

------

# 六、总口诀

```text
IaaS 租机器
PaaS 租平台
SaaS 用软件
```

------

# 七、一句话总结

> IaaS 提供基础设施，PaaS 提供开发运行平台，SaaS 提供可直接使用的软件服务。

# 业务流程建模（极简速记）

```text
业务流程建模方法主要有：
1、流程图(flow chart)，是最早用于业务流程的一种图形化描述方法，易学习、好理解，但存在无法清楚界定流程界限、不支持层次化描述业务流程等问题；
2、角色活动图(Role Activity Diagram，RAD)和角色交互图(Role Interaction Diagram，RID)，擅长描述角色与活动、角色与角色的交互关系，但不支持层次化描述业务流程；
3、IDEF0和IDEF3，IDEF0描述业务流程做什么，但没指明谁做；IDEF3回答了怎么做，但描述复杂业务流程难度大；
4、高级Petri网有很强的数学基础，可以计算／仿真分析业务流程性能，但用户的学习难度大；
5、统一建模语言(Uniform Modeling Language，UML)活动图易学习和使用，但模型的仿真和分析能力差；
6、BPMN是一种以业务流程图的形式表示业务流程的图形方法，可以用其定义的一系列业务组件，组成业务流程图
```

# 微服务架构与 SOA 对比（软考快速记忆）

# 一、一句话区别

```text
SOA：重企业应用集成
微服务：重服务自治与独立部署
```

------

# 二、基本概念

## 1. SOA

SOA 全称：

```text
Service-Oriented Architecture
```

中文：

```text
面向服务架构
```

核心：

```text
把系统功能封装成服务
通过统一接口对外提供
```

典型用途：

```text
企业内部多个系统集成
```

例如：

```text
ERP、CRM、财务系统、库存系统集成
```

------

## 2. 微服务架构

核心：

```text
把大型系统拆成多个小服务
每个服务围绕业务能力独立运行
```

例如：

```text
订单服务
支付服务
库存服务
用户服务
物流服务
```

------

# 三、核心对比（超高频）

| 对比项   | SOA                    | 微服务                       |
| -------- | ---------------------- | ---------------------------- |
| 服务粒度 | 较粗                   | 较细                         |
| 主要目标 | 企业应用集成           | 快速开发、独立部署、弹性扩展 |
| 通信方式 | ESB、SOAP、Web Service | REST、RPC、消息队列          |
| 数据管理 | 倾向共享数据           | 服务独立数据库               |
| 部署方式 | 通常不完全独立         | 每个服务可独立部署           |
| 技术栈   | 较统一                 | 可异构                       |
| 治理方式 | 集中治理               | 去中心化治理                 |
| 适用场景 | 企业系统集成           | 云原生、大型互联网系统       |

------

# 四、最重要区别 1：服务粒度

## SOA

```text
服务粒度较粗
```

例如：

```text
订单管理服务
客户管理服务
财务管理服务
```

------

## 微服务

```text
服务粒度更细
```

例如：

```text
订单创建服务
订单查询服务
订单支付服务
订单取消服务
```

------

# 五、最重要区别 2：治理方式

## SOA

常用：

```text
ESB 企业服务总线
```

负责：

```text
服务路由
协议转换
消息转换
服务编排
```

特点：

```text
集中式治理
```

------

## 微服务

常用：

```text
服务注册发现
API 网关
配置中心
消息队列
监控系统
```

特点：

```text
去中心化治理
```

------

# 六、最重要区别 3：数据管理

## SOA

```text
多个服务可能共享数据库或共享企业数据模型
```

------

## 微服务

```text
每个服务拥有自己的数据
```

例如：

```text
订单服务 → 订单库
库存服务 → 库存库
支付服务 → 支付库
```

优点：

```text
降低耦合
```

难点：

```text
分布式事务
数据一致性
```

------

# 七、两者关系

微服务可以理解为：

```text
SOA 思想的一种细粒度、轻量化、去中心化实践
```

但考试要注意：

```text
SOA ≠ 微服务
```

------

# 八、软考关键词

## SOA 关键词

```text
服务
松耦合
标准接口
企业应用集成
ESB
SOAP
Web Service
```

------

## 微服务关键词

```text
小服务
独立部署
独立运行
独立数据库
REST/RPC
去中心化
自动化运维
```

------

# 九、易混淆点

| 易错点               | 正确理解                                   |
| -------------------- | ------------------------------------------ |
| 微服务就是 SOA       | 错，微服务是更细粒度、更轻量化的服务化实践 |
| SOA 一定是微服务     | 错，SOA 服务粒度通常更粗                   |
| 微服务必须使用 ESB   | 错，微服务通常避免大型 ESB                 |
| 微服务共享数据库更好 | 错，微服务强调服务数据自治                 |

------

# 十、软考口诀

```text
SOA 重集成
微服务重自治
SOA 粗服务
微服务小服务
SOA 常用 ESB
微服务常用网关和注册中心
```

------

# 十一、一句话总结

> SOA 面向企业级服务集成，服务粒度较粗，常通过 ESB 进行集中治理；微服务是更细粒度、更轻量化的服务化架构，强调服务自治、独立部署、独立数据库和去中心化治理。

# 适用于应用服务器

```text
应用服务器：EJB + COM+
Web服务器：Servlet + ASP
```

------

# 遗留系统演化问题四象限

```text
1、改造策略
第一象限为高水平、高价值区，即遗留系统的技术含量较高且具有较高的商业价值，本身还有极大的生命力。改造策略在遗留系统的基础上，新增功能或做改进使用。
2、集成策略
第二象限 为高水平、低价值区，即遗留系统的技术含量较高，但其业务价值较低。形成了一个个信息孤岛，对这种遗留系统的演化策略为集成。
3、淘汰策略
第三象限为低水平、低价值区，即遗留系统的技术含量较低，且具有较低的业务价值。对这种遗留系统的演化策略为淘汰，即全面重新开发新的系统以代替遗留系统。
4、继承策略


第四象限 为低水平、高价值区，即遗留系统的技术含量较低，但具有较高的商业价值，对这种遗留系统的演化策略为继承。


```

------

# 基于规则的系统

```text
基于规则的系统包括规则集、规则解释器、规则/数据选择器及工作内存
```

------

# 软件架构复用

```text
软件架构复用的类型包括机会复用和系统复用，机会复用是指开发过程中，只要发现有可复用的资产，就对其进行复用。
系统复用是指在开发之前，就要进行规划，以决定哪些需要复用。
```

------

# 运行期质量属性

```text
性能：指软件系统及时提供相应服务的能力。（速度、吞吐量、持续高速性）
安全性：指软件系统同时兼顾向合法用户提供服务，以及阻止非授权使用的能力。
易用性：指软件系统易于被使用的程度。
可伸缩性：指当用户数和数据量增加时，软件系统维持高服务质量的能力。例如，通过增加 服务器来提高能力。
互操作性：与其他系统交换数据和相互调用服务的难易程度。
可靠性：在一定的时间内无故障运行的能力。
持续可用性：指系统长时间无故障运行的能力。与可靠性相关联，常将其纳入可靠性中。
鲁棒性：是指软件系统在一些非正常情况（如用户进行了非法操作、相关的软硬件系统发生了故障等）下仍能够正常运行的能力。也称健壮性或容错性。
开发期质量属性：
易理解性：指设计被开发人员理解的难易程度。
可扩展性：软件因适应需求变化而增加新功能的能力。也称为灵活性。
可重用性：指重用软件系统或某一部分的难易程度。
可测试性：对软件测试以证明其满足需求规范的难易程度。
可维护性：当需要修改缺陷、增加功能、提高质量属性时，定位修改点并实施修改的难易程度。
可移植性：将软件系统从一个运行环境转移到另一个不同的运行环境的难易程度。
```

------

# 质量属性场景

# 一、真题答案

## 问题1

质量属性场景六部分包括：

```text
刺激源
刺激
环境
制品
响应
响应度量
```

答案：

```text
A 响应和响应度量
```

------

## 问题2

题干中的刺激：

```text
想要学习系统特性
有效使用系统
使错误影响最低
适配系统
对系统满意
```

属于：

```text
易用性
```

答案：

```text
C 易用性
```

------

# 二、质量属性场景六要素（超高频）

| 要素     | 含义             |
| -------- | ---------------- |
| 刺激源   | 谁触发           |
| 刺激     | 发生什么         |
| 环境     | 在什么条件下     |
| 制品     | 影响系统哪个部分 |
| 响应     | 系统如何反应     |
| 响应度量 | 如何衡量响应     |

------

# 三、易用性刺激（高频）

易用性关注：

```text
用户能否方便、有效、满意地使用系统
```

典型刺激：

```text
学习系统特性
有效使用系统
减少错误影响
适配系统
对系统满意
```

------

# 四、口诀

## 质量属性场景六要素

```text
源刺环制响度量
```

即：

```text
刺激源、刺激、环境、制品、响应、响应度量
```

## 易用性

```text
好学
好用
少错
适配
满意
```

# 架构评估场景（极简速记）

# 一、真题

在进行架构评估时，为得出质量目标而采用的机制叫作场景。场景是从（ ）的角度对与系统的交互的简短描述。

答案：

```text
D 风险承担者
```

------

# 二、核心理解

风险承担者也可以理解为：

```text
利益相关者
```

包括：

```text
用户
架构师
开发人员
测试人员
项目管理者
运维人员
```

------

# 三、为什么不是用户

用户只是风险承担者的一类。

场景不是只从用户角度描述，而是从：

```text
所有利益相关者 / 风险承担者
```

的角度描述系统交互。

------

# 四、口诀

```text
架构评估看质量
场景来自风险承担者
```

------

# 五、一句话总结

> 架构评估中的场景是从风险承担者的角度，对其与系统交互的简短描述，用于明确质量目标。

# 5G 网络切片技术（极简速记）

# 一、真题

5G 网络采用（ ）可将 5G 网络分割成多张虚拟网络，每个虚拟网络的接入、传输和核心网是逻辑独立的，任何一个虚拟网络发生故障都不会影响其它虚拟网络。

答案：

```text
A 网络切片技术
```

------

# 二、核心概念

网络切片：

```text
把一张物理网络
切成多张逻辑独立的虚拟网络
```

每个切片可以有独立的：

```text
接入网
传输网
核心网
```

------

# 三、核心特点

```text
逻辑隔离
按需定制
故障隔离
资源独立
```

------

# 四、典型用途

不同业务使用不同切片：

```text
自动驾驶
工业互联网
高清视频
智慧医疗
物联网
```

------

# 五、易错点

| 选项         | 说明                                     |
| ------------ | ---------------------------------------- |
| 网络切片     | 正确，划分多张虚拟网络                   |
| 边缘计算     | 靠近用户侧计算，降低时延                 |
| 网络隔离     | 太泛，不是 5G 标准关键词                 |
| 软件定义网络 | 用软件控制网络，可支撑切片但不是本题答案 |

------

# 六、口诀

```text
5G 切片
一网多片
逻辑隔离
互不影响
```

# ABSD 体系结构需求（极简速记）

# 一、真题

在 ABSD（基于架构的软件开发）方法中，ABSD 体系结构需求一般来自 3 个方面：

```text
系统的质量目标
系统的商业目标
系统需求
```

问题2答案：

```text
C 开发人员的商业目标
```

------

# 二、ABSD 是什么

ABSD：

```text
Architecture-Based Software Development
```

中文：

```text
基于架构的软件开发
```

核心：

```text
以体系结构为中心进行软件开发
```

------

# 三、ABSD 需求来源（三方面）

```text
1. 系统质量目标
2. 系统商业目标
3. 开发人员的商业目标
```

注意：本题按软考参考答案记忆。

------

# 四、软考口诀

```text
ABSD 三需求：
系统质量目标
系统商业目标
开发人员商业目标
```

------

# 五、易错点

| 易错项   | 正确理解                           |
| -------- | ---------------------------------- |
| 用户需求 | 不是本题参考答案                   |
| 系统需求 | 易误选，但本题不选                 |
| 遗留系统 | 可影响架构，但不是本题三大来源之一 |

------

# 六、一句话总结

> 本题按软考参考答案记忆：ABSD 体系结构需求来自系统质量目标、系统商业目标和开发人员的商业目标。

# 4.DSSA（领域特定软件体系结构）

全称：

```text
Domain Specific Software Architecture
```

核心：

```text
针对某一领域建立可复用架构与构件
```

三阶段：

| 阶段     | 核心目标       |
| -------- | -------------- |
| 领域分析 | 获得领域模型   |
| 领域设计 | 获得 DSSA      |
| 领域实现 | 开发可复用构件 |

用途：

```
适合银行、电商、航空等具有大量共性需求的领域系统
```

---

# 软件质量属性：可用性与互操作性（极简速记）

# 一、真题答案

## 问题1

题干：

```text
关注系统在一定时间内正常工作的时间所占的比例
```

答案：

```text
A 可用性
```

------

## 问题2

题干：

```text
关注软件系统与其他系统交换数据和相互调用服务的难易程度
```

答案：

```text
互操作性
```

------

# 二、核心区分

| 质量属性 | 核心含义                           |
| -------- | ---------------------------------- |
| 可用性   | 系统正常工作的时间比例             |
| 互操作性 | 与其他系统交换数据、调用服务的能力 |
| 性能     | 响应时间、吞吐量                   |
| 安全性   | 抵抗安全威胁                       |
| 可修改性 | 修改、扩展、维护的难易程度         |
| 可靠性   | 持续无故障运行能力                 |

------

# 三、可用性关键词

```text
正常运行时间比例
故障恢复速度
系统可访问
```

常见指标：

```text
可用率
MTTR
```

------

# 四、互操作性关键词

```text
系统之间交互
数据交换
服务调用
接口兼容
```

------

# 五、口诀

```text
可用性看正常时间
互操作看系统互通
性能看快不快
安全看防不防
可修改看好不好改
可靠看稳不稳
```

# SAEM 架构评估方法（极简速记）

# 一、SAEM 是什么

SAEM 通常理解为：

```text
Software Architecture Evaluation Model / Method
```

中文：

```text
软件体系结构评估模型 / 方法
```

核心：

```text
对软件体系结构质量进行评价
```

------

# 二、软考定位

```text
SAEM 不是软考最常考的正确答案
常作为干扰项出现
```

尤其是效用树相关题：

```text
效用树属于 ATAM
不是 SAEM
```

------

# 三、与常见方法对比

| 方法 | 核心                     |
| ---- | ------------------------ |
| SAAM | 可修改性分析             |
| ATAM | 质量属性权衡分析         |
| CBAM | 成本收益分析             |
| SAEM | 架构评估模型，常作干扰项 |

------

# 四、易错点

| 题目关键词   | 正确答案 |
| ------------ | -------- |
| 效用树       | ATAM     |
| 可修改性分析 | SAAM     |
| 成本收益分析 | CBAM     |
| 架构评估模型 | SAEM     |

------

# 五、口诀

```text
SAAM 看修改
ATAM 看权衡
CBAM 看成本
SAEM 常干扰
```
